<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Theme applied to the Android Window while the process is starting
         when the OS's Dark Mode setting is off -->
    <style name="LaunchTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <!-- Show a splash screen while Flutter initializes -->
        <item name="android:windowBackground">@drawable/launch_background</item>
    </style>

    <!-- Theme applied once the app has started (used by Flutter embedding) -->
    <style name="NormalTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <!-- Use black background to avoid flicker with AR views -->
        <item name="android:windowBackground">@android:color/black</item>
    </style>

    <!-- Theme for CameraKit or native AR activities -->
    <style name="AppTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentTransitions">true</item>
        <item name="android:windowBackground">@android:color/black</item>
        <!-- Optional: smooth fade when switching to/from AR -->
        <item name="android:windowAnimationStyle">@android:style/Animation</item>
    </style>

</resources>
